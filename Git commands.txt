- git init - To initialize or reinitialize the repo
- git status - To see status and modification of local repo
- git --help - Help desk

Staging/Index
local repo
working area

- git add - adding to staging
- git commit - giving the message
- git log - to see the commits
- git checkout <commit id> - to move the head/cursur to previous or another commit which i want
- cat ~/.gitconfig - Contains the email and name of git user
- git config --global user.name "Username" - Giving the username
- git config --global user.email "someemail" - Giving the email
- git remote add origin https://github.com/zaynazar/Azar.git - To connect with remote repository
- git remote -v - gives the origin variable and its value
- git push <where you need to push, i.e, Origin> <Branch>
----------------------------------------------------------------
- git clone https://github.com/zaynazar/Azar.git - 
test
